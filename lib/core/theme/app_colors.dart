import 'package:flutter/material.dart';

/// Shared palette so every new screen matches the look of the Auth
/// feature. Feel free to move this into a proper ThemeData later.
class AppColors {
  AppColors._();

  static const background = Color(0xFFEDEDED);
  static const gold = Color(0xFFC0974C);
  static const textDark = Color(0xFF1A1A1A);
  static const hintGray = Color(0xFF8A8A8A);
  static const cardWhite = Colors.white;
  static const success = Color(0xFF2ECC71);
  static const danger = Colors.redAccent;
}
